import { AuthentificationImpl } from './authentification-impl';

describe('AuthentificationImpl', () => {
  it('should create an instance', () => {
    expect(new AuthentificationImpl()).toBeTruthy();
  });
});
